{
  "//": "Test262 root: sibling repo at same level as protoJS (e.g. proyectos/test262). Override with TEST262_ROOT env if needed.",
  "test262_root": "../test262",
  "harness_dir": "harness",
  "default_timeout_ms": 5000,
  "//patterns": "Default patterns for quick runs; override with TEST262_PATTERNS or edit for full runs.",
  "patterns": [
    "language",
    "built-ins"
  ],
  "//use_proto_eval": "When true or when TEST262_USE_PROTO_EVAL=1, runs via protoCore interpreter path (PROTOJS_USE_PROTO_EVAL=1).",
  "use_proto_eval": true
}