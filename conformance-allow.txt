# conformance-allow.txt -- the static ratchet for protoJS.
#
# Format: <check-id> <path>:<line> <sha1-12-of-the-line-text> :: <justification>
# A <path>:* entry with a * hash covers WARNINGS ONLY in that file; an error must
# be justified site by site, so a blanket entry can never hide a real defect.
#
# Run: python3 ../protoCore/scripts/conformance/check_static.py --repo .
# Exit 0 clean, 1 an unjustified finding, 2 a stale entry.
#
# THIS FILE EXITS 1 ON PURPOSE, with FOURTEEN uncovered errors.  They are real
# defects and they are left uncovered so the check keeps failing until they are
# fixed.  See docs/CONFORMANCE.md for each one's mechanism and severity.  In
# short:
#
#   EIGHT external_finalizer errors.  Five finalizers do things the contract
#   forbids absolutely, and they run ON THE SINGLE GC THREAD INSIDE THE SWEEP:
#     HTTPModule.cpp:120  freeServerState -- thread.join() AND rs->remove()
#     HTTPModule.cpp:731  freeClientRequestState -- rs->remove()
#     NetModule.cpp:213   freeServerState -- join (via teardownServer) + rs->remove()
#     NetModule.cpp:224   freeSocketState -- join (via teardownSocket) + rs->remove()
#     WorkerThreadsModule.cpp:357 freeWorkerState -- join a thread that owns its
#         OWN ProtoSpace, rs->remove(), and workerWrapper.reset(), which runs a
#         whole ~JSContextWrapper (destroyRootSet, a recursive_mutex, two pool
#         shutdowns with their own joins) from inside a GC callback
#   plus GCBridge.cpp:676, a null finalizer whose comment says the pointer is not
#   owned -- probably legitimate, but it is a C7 question nobody has answered.
#
#   SIX blocking_join_unbracketed errors.  ThreadPoolExecutor.cpp:43 and :64 are
#   reached from ~JSContextWrapper on the main thread, which IS registered, with
#   no UnmanagedScope; HTTPModule.cpp:130, NetModule.cpp:199 and :209 and
#   WorkerThreadsModule.cpp:362 are the joins inside the finalizers above.
#   Note the inconsistency that makes this a finding rather than an oversight:
#   the SAME teardown helpers are bracketed when called from JavaScript
#   (NetModule.cpp:401 and :649) and not when called from the finalizer.
symbol_key_source src/ArrayBufferPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/ArrayPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/BigIntPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/BooleanPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/DataViewPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/DatePrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/EventLoopBindings.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/ExecutionEngine.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/FunctionPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/IteratorPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/JSONBuiltin.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/MapPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/MathBuiltin.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/NumberPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
attr_sentinel src/ObjectPrototype.cpp:* * :: File-level entry covering the attr_sentinel WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/ObjectPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/PromisePrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/ProtoCoreNativeBindings.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
attr_sentinel src/ProtoDeferred.cpp:* * :: File-level entry covering the attr_sentinel WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/ProtoDeferred.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/ProtoNativeModule.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/PrototypeUtils.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
attr_sentinel src/ProxyBuiltin.cpp:* * :: File-level entry covering the attr_sentinel WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/ProxyBuiltin.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/RegExpPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/RegExpStringIterator.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/SetPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/StringPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
attr_sentinel src/StringPrototype.cpp:* * :: File-level entry covering the attr_sentinel WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/TypedArrayPrototype.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/console.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/debugging/IntegratedDebugger.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/main.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/memory/MemoryAnalyzer.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/modules/CommonJSLoader.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/modules/ProcessModule.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
attr_sentinel src/modules/child_process/ChildProcessModule.cpp:* * :: File-level entry covering the attr_sentinel WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
attr_sentinel src/modules/cluster/ClusterModule.cpp:* * :: File-level entry covering the attr_sentinel WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/modules/crypto/CryptoModule.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
attr_sentinel src/modules/dgram/DgramModule.cpp:* * :: File-level entry covering the attr_sentinel WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/modules/dgram/DgramModule.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/modules/dns/DNSModule.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/modules/fs/FSModule.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
attr_sentinel src/modules/http/HTTPModule.cpp:* * :: File-level entry covering the attr_sentinel WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/modules/http/HTTPModule.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
attr_sentinel src/modules/net/NetModule.cpp:* * :: File-level entry covering the attr_sentinel WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/modules/stream/StreamModule.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/modules/util/UtilModule.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/monitoring/Metrics.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
attr_sentinel src/native/NativeModuleABI.cpp:* * :: File-level entry covering the attr_sentinel WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/profiling/Profiler.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
symbol_key_source src/runtime/ProtoInterpreter.cpp:* * :: File-level entry covering the symbol_key_source WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
attr_sentinel src/runtime/ProtoInterpreter.cpp:* * :: File-level entry covering the attr_sentinel WARNINGS in this file. For symbol_key_source these are uninterned strings reaching setAttribute or getAttribute as the name: protoCore auto-interns on the write path and falls back to a content lookup on the read path, so every one of them is CORRECT and pays a per-call intern or a content hash over the rope -- a performance finding, not a miss. The count is large (197 across the tree) and reducing it is protoJS own next phase; what matters for conformance is that NONE of them reaches getOwnAttributeDirect, which is the destination where an uninterned key silently misses. For attr_sentinel they are heuristic boolean-test hits whose false-positive rate the check documents as unbounded from the text. A file-level entry is deliberate: 217 individual justifications would be a ritual, not a review. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
